#include<stdio.h>

int main ()
{
    float a=9, b=6, x=10, y=7, jumlah;
    jumlah= (a+b)*x/y;
    printf("Variabel a bernilai %.f\n", a);
    printf("Variabel b bernilai %.f\n", b);
    printf("Variabel x bernilai %.f\n", x);
    printf("Variabel y bernilai %.f\n", y);
    printf("Hasil dari a ditambah b dikali x dibagi y adalah %.2f\n", jumlah);
}

